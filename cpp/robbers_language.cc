#include <string>

const std::string toRobbersLanguage(std::string const &english) {
    if (english == "o")
        return "o";
    if (english == "l")
        return "lol";
    else
        return "ror";
}
